#include <iostream>

using namespace std;

void BFS(){

}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int n;
    pair<int,int> tom,jerry;
    cin>>n;
    int xy[n][n];
    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++){
            xy[i][j];
        }
    }
    cin>>tom.first>>tom.second;
    cin>>jerry.first>>jerry.second;
    BFS();
    return 0;
}